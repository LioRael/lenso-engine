[package]
name = "lenso-engine-markdown"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Optional Markdown resource processor for Lenso Engine"

[dependencies]
lenso-engine = { path = "../lenso-engine", version = "0.1.0" }
anyhow = "1"
serde_json = "1"
